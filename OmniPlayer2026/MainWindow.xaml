<Window x:Class="OmniPlayer2026.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:OmniPlayer2026.Controls"
        Title="OmniPlayer 2026"
        Width="1280"
        Height="720"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
  <Border Background="#CC0A0C10" CornerRadius="18" Padding="16">
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>

      <DockPanel Grid.Row="0" LastChildFill="False" Margin="0,0,0,12">
        <TextBlock Text="OmniPlayer 2026"
                   Foreground="White"
                   FontSize="22"
                   FontWeight="SemiBold"
                   DockPanel.Dock="Left" />
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
          <Button Content="—" Click="OnMinimizeClicked" Margin="0,0,6,0" />
          <Button Content="□" Click="OnMaximizeClicked" Margin="0,0,6,0" />
          <Button Content="✕" Click="OnCloseClicked" />
        </StackPanel>
      </DockPanel>

      <Border Grid.Row="1"
              Background="#161A21"
              CornerRadius="12"
              AllowDrop="True"
              Drop="OnDropFiles"
              DragOver="OnDragOver">
        <ItemsControl x:Name="PlayersHost">
          <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
              <UniformGrid Columns="3" />
            </ItemsPanelTemplate>
          </ItemsControl.ItemsPanel>
        </ItemsControl>
      </Border>
    </Grid>
  </Border>
</Window>
